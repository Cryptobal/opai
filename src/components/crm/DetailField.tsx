/**
 * Re-export desde el primitivo compartido en `@/components/opai`.
 * Mantenido por compatibilidad con imports relativos `./DetailField` desde el
 * resto de los archivos del módulo CRM.
 */
export { DetailField, DetailFieldGrid } from "@/components/opai/DetailField";
