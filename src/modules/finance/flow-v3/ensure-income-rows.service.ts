/**
 * @deprecated v4.1 — usar `reconcileIncomeRows` desde
 * `./reconcile-income-rows.service`. Este archivo solo reexporta para no
 * romper imports residuales.
 */
export {
  reconcileIncomeRows,
  ensureIncomeRows,
} from "./reconcile-income-rows.service";
