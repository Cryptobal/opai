import { CorreosClient } from "@/components/crm/correos/CorreosClient";

export const metadata = { title: "Correos · Comercial" };

export default function CorreosPage() {
  return <CorreosClient />;
}
