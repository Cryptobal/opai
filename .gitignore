# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
.next_backup*/
/out/

# production
/build

# uploads (user-generated)
public/uploads/

# misc
.DS_Store
*.pem

# Certificates digitales y scratch local de scripts/factoring
# (extract-cert.ts genera .pfx desde la BD para uso local del sandbox).
# NUNCA commitear estos archivos — contienen firma digital de la empresa.
/tmp/
*.pfx
*.p12

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
.env.sentry-build-plugin

# vercel
.vercel
.env.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/dev.db
prisma/dev.db-journal

# terminals (Cursor)
terminals/

# nested repo (cotizador inteligente - proyecto separado)
gard-docs/

.claude/
node_modules_bad_1771520600/
.next_old/
.worktrees/

# Capacitor native projects (regenerated with `npx cap add`)
# These are per-app working dirs populated by the cap:<app>:* scripts
# in package.json. One dir per platform per app.
android/
ios/
android-terreno/
android-personas/
android-erp/
ios-terreno/
ios-personas/
ios-erp/

# iCloud/Dropbox/OneDrive sync conflict artifacts
* 2.*
* 3.*
* 4.*

# Personal tooling and scratch (Claude Code superpowers, brainstorming notes)
.superpowers/

# Private operational data (PII — must never be committed)
# Lives in private storage (see docs/01-architecture/data-sources.md)
Datos Ops/
/private-data/

# ═══════════════════════════════════════════════════════════════════
# Data files con PII potencial — NUNCA commitear
# Si necesitás samples para documentación, ponelos en
# docs/02-implementation/payroll/sample-formats/ con datos anonimizados.
# ═══════════════════════════════════════════════════════════════════

# Carpetas comunes de data operativa
Datos Ops/
datos-ops/
Datos/
/data/
/datos/
data-private/
private-data/
/uploads-local/

# CSVs en raíz del repo (los samples válidos viven en docs/)
/*.csv
/*.CSV

# Excels en raíz
/*.xlsx
/*.xls
/*.XLSX
/*.XLS

# Dumps de base de datos
*.sql.gz
*.dump
backup-*.sql
dump-*.sql
db-export-*.sql

# Filenames sospechosos de PII chilena (libros de payroll, F30, Previred)
*libro_remuneraciones*
*libro_imposiciones*
*Libro_Remuneracion*
*Libro_Imposicion*
*LIBRO_REMUNERACION*
*LIBRO_IMPOSICION*
LIBRO_*
*previred*
*Previred*
*PREVIRED*
*F30*
*nomina*
*Nomina*
*remuneraciones-*.csv
*imposiciones-*.csv
pnpm-lock.yaml
